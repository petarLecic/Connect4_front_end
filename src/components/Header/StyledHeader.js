import styled from 'styled-components'

export const StyledHeader = styled.header`
    img {
        margin: 15px 165px;
        width: 370px;
        &:hover {
            cursor: pointer;
        }
    }
` 